export enum COLOR {
	'primary' = '#00ab8c',
	'black' = '#000000',
	'lightGray' = '#a1a1aa',
	'superLightGray' = '#d1d5db',
	'white' = '#ffffff',
	'red' = '#dc2626',
	'blue' = '#3b82f6',
	'yellow' = '#eab308',
	'gray' = '#6b7280',
}

export enum BUTTON_COLOR {
	'default' = COLOR.white,
	'info' = COLOR.white,
	'alert' = COLOR.white,
	'warning' = COLOR.black,
	'disabled' = COLOR.white,
}

export enum BUTTON_BG_COLOR {
	'default' = COLOR.primary,
	'info' = COLOR.blue,
	'alert' = COLOR.red,
	'warning' = COLOR.yellow,
	'disabled' = COLOR.gray,
}

export enum INVERTED_BUTTON_COLOR {
	'default' = COLOR.primary,
	'info' = COLOR.blue,
	'alert' = COLOR.red,
	'warning' = COLOR.yellow,
	'disabled' = COLOR.gray,
}

export enum INVERTED_BUTTON_BG_COLOR {
	'default' = COLOR.white,
	'info' = COLOR.white,
	'alert' = COLOR.white,
	'warning' = COLOR.white,
	'disabled' = COLOR.white,
}