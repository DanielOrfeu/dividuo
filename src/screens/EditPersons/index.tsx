import { Text, View } from 'react-native';

export default function EditPersons({ navigation }) {
    return (
        <View className='flex-1 w-full justify-center items-center p-4'>
            <Text>EditPersons Screen</Text>
        </View>
    );
}